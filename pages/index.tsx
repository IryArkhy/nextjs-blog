import React from 'react'

const index = ({ posts, getPosts }: any): JSX.Element => {
  // useEffect(() => {
  //   getPosts();
  // }, []);

  return (
    <div>
      Hello world
    </div>
  );
};

export default index;
